# Core
torch>=2.1            # BFloat16 autocast, torch.amp
torchvision>=0.16     # ResNet-34 ImageNet weights for the 2D branch
numpy>=1.24
scipy>=1.10           # Butterworth filtering (NOT used for CWT — see note)
PyWavelets>=1.4       # CWT scalograms + wavelet baseline removal
wfdb>=4.0             # MIT-BIH .dat/.hea/.atr reading
scikit-learn>=1.3     # metrics

# NOTE: scipy.signal.cwt / morlet2 were REMOVED in scipy >= 1.15. The scalogram
# generator uses PyWavelets (pywt.cwt, 'morl') instead. scipy is still needed
# for the Butterworth band-pass filter only.
